name: PR
on: [pull_request]

permissions:
  id-token: write
  contents: read

jobs:
  call-build:
    uses: ./.github/workflows/build.yml
    secrets:
      VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}
