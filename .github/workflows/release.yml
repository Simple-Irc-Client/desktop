name: Release
on:
  release:
    types:
      - created

jobs:
  call-build:
    uses: ./.github/workflows/build.yml

  release:
    runs-on: ubuntu-latest
    steps:
      - name: publish
        run: npm run publish
