name: Release
on:
  release:
    types:
      - created

jobs:
  build:
    steps:
      - uses: ./.github/workflows/build.yml
        secrets: inherit

  release:
    steps:
      - name: publish
        run: npm run publish
