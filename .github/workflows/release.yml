name: Release
on:
  release:
    types:
      - created

permissions:
  id-token: write
  contents: read
      
jobs:
  call-build:
    uses: ./.github/workflows/build.yml
    with:
      publish: true
    secrets:
      VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}
