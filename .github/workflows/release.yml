name: Release
on:
  release:
    types:
      - created

jobs:
  call-build:
    uses: ./.github/workflows/build.yml
    secrets:
      SIC_PAT: ${{ secrets.SIC_PAT }}
    inputs:
      publish: true
