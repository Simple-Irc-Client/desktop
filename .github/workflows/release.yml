name: Release
on:
  release:
    types:
      - created

jobs:
  call-build:
    uses: ./.github/workflows/build.yml

  release:
    runs-on: ubuntu-latest
    needs: [Build]
    steps:
      - name: publish
        run: npm run publish
